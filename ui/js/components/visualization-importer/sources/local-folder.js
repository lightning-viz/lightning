var React = require('react');

var styles = {
};

var NPM = React.createClass({

    render: function() {
        return (
            <div>
            local folder
            </div>
        );
    },
});

module.exports = NPM;
